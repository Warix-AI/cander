/** Runtime copy for Build project panes (hosting / models / keys). */
export const developmentIntegrated =
  "Backend, models, and keys are wired into this project already. We provision what it needs — no copying keys or separate setup.";
