"use client";

/**
 * Explore (`research`) projects now live on the unified Canvas dashboard.
 * Prefer `@/components/spaces/CreateDashboard`.
 */
export { CreateDashboard as ResearchDashboard } from "@/components/spaces/CreateDashboard";
